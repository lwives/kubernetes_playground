apiVersion: v1
kind: Service
metadata:
  name: product-service-clusterip # Nome DNS para outros microsserviços se conectarem
  labels:
    app: product-service
spec:
  selector:
    app: product-service
  ports:
    - protocol: TCP
      port: 8002
      targetPort: 8002
  type: ClusterIP # Acessível apenas dentro do cluster